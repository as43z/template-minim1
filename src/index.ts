// MAIN
// start imports
/**
 * app
 * startConnection
 */
// end imports

async function main() {
    // base de datos:

    // PORT and BASE URI
    const PORT = '';
    const baseAPI = `http:localhost:${PORT}/`;

    // listen


    console.log(`[ SERVER STARTED ] server started @ ${baseAPI}`);
}

main()